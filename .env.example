# ========== 数据库（必填）==========
# 本地：建议库名 happy_dev，与线上区分。Windows 安装到 E:/ 等非 C 盘见 docs/postgresql-windows-non-c.md
# 密码改为安装 Postgres 时为 postgres 用户设置的密码（示例里的 postgres 仅作占位）。
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/happy_dev"
# 迁移 prisma migrate * 使用；本机常与 DATABASE_URL 相同。
# Neon：Pooled → DATABASE_URL，Direct → DIRECT_URL（Vercel 两处都要配）。
DIRECT_URL="postgresql://postgres:postgres@127.0.0.1:5432/happy_dev"

# ========== 可选：演示用户 ==========
# DEFAULT_USER_ID=
# seed 创建 demo 用户时的密码（默认 demo123）
# SEED_DEMO_PASSWORD=demo123

# ========== 可选：登录（默认关闭 = 演示模式）==========
# AUTH_ENABLED=true
# 开启登录后必填其一（生产/Vercel 同样要配）：
# AUTH_SECRET=
# NEXTAUTH_SECRET=
# 局域网访问登录：与地址栏完全一致。建议同时写两行（同值），因客户端库只认 NEXTAUTH_URL：
# AUTH_URL=http://192.168.x.x:3000
# NEXTAUTH_URL=http://192.168.x.x:3000
# 若只写 AUTH_URL，next.config 会尝试同步到 NEXTAUTH_URL；仍失败请检查终端是否 export 过旧的 NEXTAUTH_URL。
# 局域网 dev：Next 会拦截 dev-only 资源；填写浏览器地址栏的 host（逗号/空格分隔），勿用 CIDR。
# ALLOWED_DEV_ORIGINS=192.168.x.x,192.168.x.x:3000
